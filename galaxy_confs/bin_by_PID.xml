<tool id="bin_by_PID"  name="Bin sequences by PID">

	<description> bin sequences by PID, this only works for Colin's data ATM</description>

	<command>
	/opt/exp_soft/galaxy-tools/custom_tools/bin/bin_by_PID.py

	## required input
        --f     $input_f

	</command>

        <inputs>

                <param name="input_f" type="data" format="fastq" label="Select input FASTA alignment file"/>

	</inputs>

        <outputs>

                <data format="zip" name= "out_zip"    label="${tool.name} on ${on_string}: bins"   
                from_work_dir="bins.zip"    /> 

	</outputs>

        <help>

** Binner overview **

Bins sequences by PID. Only works for Colin's data at the moment. Great things will come from this. In 2014

        </help>

</tool>

